{
  "name": "aso-dashboard-server",
  "version": "1.0.0",
  "description": "Backend aggregation server for ASO Suggestion Lifecycle dashboard",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test test/*.test.js",
    "snapshot": "node scripts/snapshot.js",
    "snapshot:refresh": "node scripts/snapshot.js && curl -s -X POST http://localhost:3001/api/snapshot/reload && echo ' Snapshot reloaded.'",
    "snapshot:customers": "node scripts/snapshot-customers.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
